name: location service ci

on:
  push:
    branches: ["**"]
    paths:
      - "location/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/location-ci.yaml"
      - "pom.xml"

  pull_request:
    branches: ["main"]
    paths:
      - "location/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/location-ci.yaml"
      - "pom.xml"

  workflow_dispatch:

jobs:

  Build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./.github/workflows/actions

      - name: Maven Build
        run: mvn clean install -pl location -am -DskipTests

      - name: Checkstyle
        run: mvn checkstyle:checkstyle -pl location -am -Dcheckstyle.output.file=location-checkstyle-result.xml

      - name: Upload Checkstyle Result
        uses: jwgmeligmeyling/checkstyle-github-action@master
        with:
          path: '**/location-checkstyle-result.xml'

      - name: Login GHCR
        if: ${ github.ref == 'refs/heads/main' }
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${ github.actor }
          password: ${ secrets.GITHUB_TOKEN }

      - name: Build Docker Image
        if: ${ github.ref == 'refs/heads/main' }
        uses: docker/build-push-action@v6
        with:
          context: ./location
          push: true
          tags: ghcr.io/nashtech-garage/yas-location:latest

  Test:
    needs: Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/actions

      - name: Run Tests + Generate Coverage
        run: mvn verify -pl location -am

      - name: Verify Jacoco Exists
        run: ls -R location/target/site/jacoco || true

      - name: Upload Jacoco Report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report-location
          path: location/target/site/jacoco/jacoco.xml
          retention-days: 1

      - name: Test Report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Location-Unit-Test
          path: "location/**/*-reports/TEST*.xml"
          reporter: java-junit

  SonarCloud:
    needs: Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./.github/workflows/actions

      - name: Download Jacoco
        uses: actions/download-artifact@v4
        with:
          name: jacoco-report-location
          path: location/target/site/jacoco

      - name: Run SonarCloud
        id: sonar
        continue-on-error: true
        env:
          SONAR_TOKEN: ${ secrets.SONAR_TOKEN }
        run: >
          mvn org.sonarsource.scanner.maven:sonar-maven-plugin:sonar
          -pl location -am
          -Dsonar.projectKey=nashtech-garage_yas-location
          -Dsonar.organization=nashtech-garage
          -Dsonar.host.url=https://sonarcloud.io
          -Dsonar.coverage.jacoco.xmlReportPaths=location/target/site/jacoco/jacoco.xml

      - name: Sonar Summary
        if: always()
        run: |
          if [ "${{ steps.sonar.outcome }}" = "success" ]; then
            ICON="🟢"
            STATUS="PASS"
          else
            ICON="🔴"
            STATUS="FAIL"
          fi

          echo "## SonarCloud location" >> $GITHUB_STEP_SUMMARY
          echo "| Status | Result |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Scan | $ICON $STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "https://sonarcloud.io/project/overview?id=nashtech-garage_yas-location" >> $GITHUB_STEP_SUMMARY

  Coverage:
    needs: Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download Jacoco
        uses: actions/download-artifact@v4
        with:
          name: jacoco-report-location
          path: target/jacoco-results

      - name: Coverage Report
        id: jacoco
        uses: madrapps/jacoco-report@v1.6.1
        with:
          paths: target/jacoco-results/jacoco.xml
          token: ${ secrets.GITHUB_TOKEN }
          min-coverage-overall: 80
          min-coverage-changed-files: 60
          title: "Location Coverage Report"
          update-comment: true

      - name: Coverage Summary
        if: always()
        run: |
          COVERAGE="${{ steps.jacoco.outputs.coverage-overall }}"
          CHANGED="${{ steps.jacoco.outputs.coverage-changed-files }}"

          [ -z "$COVERAGE" ] && COVERAGE=0
          [ -z "$CHANGED" ] && CHANGED=0

          echo "## Coverage location" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|------:|" >> $GITHUB_STEP_SUMMARY
          echo "| Overall | $COVERAGE% |" >> $GITHUB_STEP_SUMMARY
          echo "| Changed Files | $CHANGED% |" >> $GITHUB_STEP_SUMMARY
